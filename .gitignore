# 密钥 / 运行时配置（绝不入库）
.env
.env.*
*.local
secrets.*.yml
credentials*

# 依赖 / 构建产物
node_modules/
__pycache__/
*.pyc
.venv/
dist/
build/

# 编辑器 / OS
.DS_Store
.idea/
.vscode/
*.swp

# WorkBuddy 工作目录（本地记忆/运行态，非项目治理证据，绝不入库）
.workbuddy/

# 外来 skill 工件（非本项目交付）
.career-os/

# 本地开发 env（含本地生成密钥，绝不入库）
apps/api/.env.local

# 本地验证 / 构建产物（非交付，绝不入库）
_diag_verify.log
scripts/_verify_rc.txt
scripts/_verify_run*.log
scripts/_tmp_test_parser.py
scripts/fact_consistency_results.json
tsconfig.tsbuildinfo
apps/web/dist-check/
typescript-typescript-win32-x64-7.0.2.tgz
