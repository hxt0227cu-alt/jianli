# 2026-08-03 工作日志

## 需求访谈 + 产出 PRD（个人 AI 问答网站）
- 用户目标：搭建"个人 AI 问答网站"——混合「个人作品集站点 + 云端自托管 RAG 问答 + 面试预约 agent + 简单管理后台」。
- 关键决策（经多轮引导访谈确认）：
  - 问答引擎按 **RAG 检索增强**落地（PDF 切片→向量库→检索+生成+内容护栏），"训练自己的大模型"先以 RAG 实现，微调作可选增强。
  - 只答知识库范围内问题，越界明确拒绝。
  - 双入口：悬浮聊天窗 + 独立"AI 问答"页。
  - 预约 agent：意图识别自动引导 + 页面"预约面试"按钮；收集 姓名/公司/应聘岗位/期望时间/联系方式；用户自有邮箱+授权码 SMTP 发信。
  - 管理后台：登录后上传 PDF、看对话、看预约。
  - 纯中文、简洁清爽（参考豆包）；内容均用占位，后续替换。
  - 云端部署（本机不搭服务器）；云服务商待确认。
- 产出文件：`C:\Users\hxt02\Desktop\jianli\个人AI问答网站-需求文档初稿.md`（含版本记录、背景、目标、场景、功能需求、非功能需求、验收、交付物、待确认清单、实施阶段建议）。
- 备注：用户曾要求"找专家或 GitHub 开源 skill 引导需求"。市场无完美匹配（cloudbase 偏搭建、req-doc-writer 偏文档生成但假定已有资料）；最终由我直接引导访谈并产出文档。

## 需求文档修订（同日下午）
- 用户修正痛点：简历篇幅有限 → 面试官访问网站针对简历内容深挖问答。
- 预约闭环明确：大模型引导留信息 → SMTP 发邮件 + 更新"约面试表"（系统内列表 + 同步外部表格，两者都要）。
- 问答鉴权定为对外公开、无需登录（类比京东/美团智能客服）。
- 文档 v1.0 初稿已同步更新（痛点、场景1/3、R4、§4.3、§4.5、§5安全、§6验收、§8.1阶段、§8.2待确认）。

## 需求文档第三轮修订（16:27）
- 布局升级：问答核心页左右分栏（左简历按模块拆块展示，右大模型问答窗口），面试官边看边问；保留右下角悬浮窗。
- 新增 R7 推荐问题/示例问答版块（基于简历动态生成，可一键提问）。
- 新增 R8 预约时弹出可视化面试排期图表（课程表式），展示已排/空闲时段，减少 BOSS 沟通成本。
- 场景1/3、§1.3、§4.2、§4.3、§4.6（新增）、§6、§7、§8.1、§8.2、§8.3 同步更新。
- 仍待确认：排期图表粒度、推荐问题来源（动态vs静态）、悬浮窗是否保留。

## 需求文档 v1.1 修订（16:39）— 人格层 / 数字分身
- 用户提出：训练的大模型应有"自己的特性"，面试官能感到在跟真人对话（本人情商、思维逻辑）。本质=把"我"蒸馏成一份人格配置。
- 新增 R9【核心】人格层（数字分身）：RAG 事实作答之上叠加用户专属人格（第一人称、表达风格、情商、思维逻辑），越界仍以本人口吻拒答、不编造事实。
- 新增 §4.7 人格层：persona card + 风格样本 few-shot；落地三级路径 L1 提示词(prompt)→L2 LoRA 轻量微调→L3 全参微调。MVP 用 L1 即可初步成立"本人感"。
- 场景1/2、§1.3、§4.7(新增)、§5、§6、§7、§8.1、§8.2、§8.3 同步更新。
- 关键判断：用户"训练自己的模型"诉求优先以 L1（人格提示词）落地，"本人感"即初步成立；L2/L3 微调视语料/算力再评估，不阻塞 MVP。

## 需求文档 v1.2 修订（16:43）— 预约闭环实时动态化
- 用户要求：约面试表实时更新（提交后立即动态刷新）；对话中由大模型引导"面试表"入口；入口进入新页面"动态面试表页"（课程表式可视化+页内可继续对话）；确认预约意向后大模型给出当前可预约时段供选择。
- 新增 R8 重定义为「动态面试表页」；R4 增加入口引导+可预约时段+实时更新；场景3重写为 4 步流程；新增 §4.8 动态面试表页（可视化+实时+页内对话+数据来源+双向可达）；实时机制建议 WebSocket/SSE。
- §1.3、§4.3（重排功能点）、§6 验收、§8.1 阶段3、§8.2 待确认（排期粒度+实时机制）、§8.3 术语表（排期图表/动态面试表页/实时刷新）同步更新。
- 关键设计：写入端→数据层(系统内列表+外部表格)→推送通道→动态表页即时渲染；外部表格同步可异步，站内列表实时性为准。

## 用例规约（16:58）— 写代码前补齐分析工件
- 用户强调：应先完成软件工程全部分析与设计步骤再写代码，目前距编码尚有未完成项。
- 产出新文件 `个人AI问答网站-用例规约.md`：参与者(面试官/候选人/问答引擎/预约Agent/邮件/外部表格)、业务场景清单(S1–S11，指出S1浏览站点、S7页内对话尚未单列进需求文档§3)、文字用例图、UC-01~UC-11逐条规约(前置/后置/基本流/扩展流/业务规则/优先级)、需求可追溯性、以及「写代码前仍需补齐的工件」10项清单(关闭待确认/评审/领域模型/架构/接口/存储/UI线框/非功能量化/测试计划/技术选型)。
- 关键判断：S1(浏览站点)与S7(动态表页内继续对话)目前仅以功能需求形式存在，未作为独立场景写入需求文档§3，属写码前需补项。

## 需求文档 v1.3 + 用例规约 v1.1（19:35）— 三大页面/登录/交互排表/微信+飞书
- 用户重大扩充：明确三大页面结构——P1 简历问答页(左简历/右问答/推荐问题/左下角简历下载按钮)、P2 项目展示页(HTML动效+右问答)、P3 动态面试表页(交互绿空闲/红已约,需登录)。
- 新增关键需求：面试官登录注册(解决"改预约路由难")；点绿格填表(公司名/公司面试邮箱/线上软件/会议号)→绿转红+信息加密隐藏；仅本人可改/取消自己红格；一家公司一时段(重复提醒)；微信预约提醒(新/改/取消)；飞书多维表格作候选人完整视图(网页内不做权限管理)。
- 重写 `需求文档初稿.md`→v1.3(R1–R16, S1–S13, §8.4 设计补充: 飞书vs更好方法推荐 + 15条补充点)；重写 `用例规约.md`→v1.1(UC-01~UC-15, S1–S13, 可追溯性, before-coding 10项)。
- 设计决策(写入文档§8.4)：系统DB为唯一真相源→同步飞书多维表格(owner视图)+微信推送(企业微信/Server酱)提醒；红格对其他面试官仅显"已预约"无公司名；并发靠服务端原子锁。
- 仍待确认(重点是): 表格时间范围/粒度、实时机制、注册验证方式、一家公司判定维度(账号/公司名)、红格展示粒度、飞书/微信通道选型、临近提醒、admin网页视图是否仍需。
- 坚持"先分析再编码"：本轮仅更新分析文档,未写代码。

## 需求文档 v1.4 + 用例规约 v1.2（20:30）— 关闭多轮开放项
- 用户逐条拍板前轮待确认项，全部落在分析工件，未写代码：
  1. 预约必须登录(角色认证: 面试官 vs owner)，支持"记住我"免登录复访；问答仍公开。
  2. "一家公司一时段"以公司名(归一化)为主、账号为辅去重。
  3. 红格对他人仅显"已预约"无公司名。
  4. 候选人可主动标任意格不可约(红)+ 页面3顶部可编辑公告(告知HR临时有事会主动通过预留电话+称呼联系)。
  5. 并发=服务端原子锁+抢占式(先提交者得)+SSE实时刷新；可选WPS式"有人在编辑"气泡。
  6. 取消/修改均触发三通道(微信+飞书+邮箱)通知+飞书同步+释放/改格。
  7. 新增面试前临近提醒(飞书+微信+邮箱)。
  8. 课表网格精确规则：周一到周日7列；30分钟粒度；9:30–22:00(25行)；每约占1.5h=3格(最晚起始20:30)；午12–14/晚18–20/周末固定红；选中3格触红→持续悬浮"不可选当前时间段"。
  9. 表单字段精简：公司名称/会议平台/会议号/HR联系方式(电话+称呼)；表单末附固定提示文案(提交后本人微信+飞书收到,可不另发邮件邀约,但正式邀约更赞)。
  10. 不做移动端适配(桌面端)。
  11. 注册安全全保留：邮箱验证/找回/防刷/隐私说明/改会议号原地+改时间换格/飞书微信同步失败邮件兜底。
- 重写 `需求文档初稿.md`→v1.4(R1–R22, S1–S15, §4.5网格精确规则, §8.4决策记录)；重写 `用例规约.md`→v1.2(UC-01~UC-16, S1–S15, 可追溯性扩展至R17–R22)。
- 仍待确认(真剩余)：临近提醒N值、推荐问题来源(动态vs静态)、微信通道选型、记住我有效期、公司名归一化策略、人格素材、注册模板/隐私文案、云/SMTP/域名/预算/模型基座。
- 用户长期偏好已写入 ~/.workbuddy/MEMORY.md：先完成软件工程全流程分析/设计再编码,不急于写代码。

## 需求文档 v1.5 + 用例规约 v1.3（23:25）— 补充轮：会话历史/热更新/限频/云选型/文案
- 用户补充决策（全部落在分析工件,未写代码）：
  1. 页面1/2 文字可选中复制(便于面试官复制后提问) → R25。
  2. 预约表单加"备注"字段(选填) → R10。
  3. 会话历史持久化：面试官登录后问答会话跨登录保留,像豆包；未登录不保存 → R23（新增 UC-15）。
  4. 知识库热更新：增/删/改文档近实时(<1分钟)重新索引生效,无需重新部署 → R24（用户确认"就是知识库热更新"；新增 UC-16）。
  5. 临近提醒 N=10 分钟 → R18。
  6. 推荐问题来源=动态生成(非静态集) → R4。
  7. 微信推送通道=WorkBuddy 微信助理 → R13。
  8. 记住我有效期=14 天 → R19。
  9. 公司名归一化=精准匹配(去空格/统一大小写后精确比较) → R12。
  10. 人格路径=L1(prompt+few-shot),最简单稳定；L2/L3 可选 → R7。
  11. 限频防刷：用户拿不准,我提出具体建议值表(注册/登录/预约/问答/SSE 各阈值)写入需求§5,标"建议值待确认"。
  12. 云服务商：用户要推荐+价格/性能对比(稳定最重要)。经调研(2026-04 腾讯云社区五厂商六维评测)推荐**腾讯云**(94分第一,SLA99.975%,9个9)；模型基座用托管LLM API(混元HY3/DeepSeek V4,输入~1元/百万token)不自租GPU；MVP年成本≈¥800–1000。写入需求§8.5对比表+成本估算。
  13. 注册验证邮件模板 + 隐私说明文案：我草拟,写入需求§8.6,待用户润色。
- 重写 `需求文档初稿.md`→v1.5(R1–R25, S1–S17, §5限频表, §8.5云对比, §8.6文案)；重写 `用例规约.md`→v1.3(UC-01~UC-18, S1–S17, 可追溯性扩展至R23–R25)。
- 真剩余待确认：云厂商最终拍板、限频阈值确认、SMTP/域名/预算、模型基座最终、人格素材提供、微信助理/飞书授权配置、邮件/隐私文案润色。
- 仍坚持先分析再编码；下一步建议：领域模型(类图)或系统架构设计。
