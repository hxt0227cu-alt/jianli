# 2026-08-04 工作记录

## 人格层路径讨论与决策
- 用户重新探讨 L1/L2/L3 人格层；最终选择 **决策 B：L1 MVP 上线 + L2 LoRA 微调语料收集与计划**。
- 关键澄清（用户曾误解"RAG=训练权重"）：RAG 是检索增强生成、不修改权重，是其毕业设计同款（Qwen2.5+文档投喂+向量检索），属事实层标准工程；真正"训练"指 L2 LoRA / L3 全参微调（人格层）。
- 用户顾虑：简历项目若只说"调用 API"显得 low。澄清：RAG 管线+人格层+实时排期+加密+三通道通知+微信/飞书对接本身就是完整 AI 产品工程，不 low；并建议若想简历更硬可做一次最小 LoRA 实验并 A/B 对比 L1/L2，L1 始终作 fallback 保稳定。
- 产出《个人AI问答网站-L2人格语料收集清单.md》（9 类语料、格式、脱敏、训练计划、盲测评估、进度追踪）。
- 需求文档升 v1.6：§4.9、R7、模型方案、待确认、决策摘要均标注决策 B 与 L2 计划。

## 状态
- 仍在"先分析再编码"阶段，未写代码。
- 下一步（待用户选）：① 起草 L1 persona card（含第9类"说话画像"访谈）；② 进入领域模型/架构设计；③ 关闭剩余待确认项（云/限频/凭证/模型基座）。

## 需求文档 v1.7 锁定（用户本轮拍板 + AI Infra 映射）
- 用户确认：**模型基座=DeepSeek（DS）API（主），混元作 Fallback**；**云=腾讯云短期（不买年，仅面试期1-2月包月可释放）**；**限频=采用建议值表**；**注册邮件+隐私=采用§8.6草拟版**。
- 用户要求工程必须体现 **AI Infra（9层纵向+4横切）**，类比其转发文章。已在需求文档新增 **§4.10 AI Infra 架构映射**：把 L0–L8 + 安全治理/CI-CD/FinOps/DevEx 四横切逐一映射到本项目，给出"便宜稳定短期可释放"选型（腾讯云+DS API），含"一次完整 Agent 调用"示例与三阶段路线图对齐（本项目主要落原型期，重资产生产期可选）。
- §5 成本改为"面试期1-2月≈¥170-200"；§8.5 新增短期成本明细（包月¥55/月×2+域名¥50+API¥20-60）；§8.2 待确认拆分为"已确认"与"仍待提供"两类；文档各处模型顺序统一为 DS 主/混元 Fallback。
- 修复：版本表重复 v1.6 行。

## 需求文档 v1.8：十二大工程映射（用户转发文章整合）
- 用户再转发「企业 Agent 落地十二大工程」文章（6 能力主链 Prompt→Context→Skill→Agent→Harness→Loop + 6 横切 Data/Ontology/Process/Evaluation/Safety/Product）。
- 新增 **§4.11 Agent 落地十二大工程映射**：将 12 项逐一对齐本工程（如 Harness=运行中心、Loop=热更新+盲测、Ontology=面试/时段/公司实体建模、Process=端到端预约流程+例外）；附 **Harness 15 步执行链（预约面试示例）** 与模型路由策略。
- 关键整合：§4.10 九层(技术栈视角) ↔ §4.11 十二大工程(能力成熟度视角) 互为透镜，新增对照表；明确 MVP 硬底线 = Harness + Safety/Governance + Product/UX 三项。
- 仍未写代码，停在分析阶段。下一步方向：领域模型(类图)/系统架构设计/起草 persona card。

## 需求文档 v1.9：评审整改版（用户提交系统性评审，P0 全闭环）
- 用户提交了专业评审（P0×7 / P1×10 / P2×5 + 设计工件×5 + MVP范围 + 终审意见"需求方向通过、开发准入暂缓"）。已将文档升 v1.9 闭环：
  - P0-1 决策状态统一（新增四种状态规范：已确认/待确认/已废弃/延后实施；§5限频改"已确认"、§8.6改"基线已确认仅润色"）。
  - P0-2 预约日期/时区/节假日：系统时区UTC+8、时间戳存储、展示当日+未来两周14天、不可约当天、最短提前1天、翻页、法定节假日自动不可约/调休可约、节假日来源+人工覆盖。
  - P0-3 改期原子事务（锁新段→事务内更新→占新→释旧→提交→通知，新段不可用原预约不变）。
  - P0-4 候选人查看权限=方案B（飞书日常视图 + 后台只读应急完整视图），统一场景10/17、§4.7、R14/R14b、术语、验收、角色权限矩阵。
  - P0-5 新增§8.7 数据分类/留存/删除（6类数据，密钥/脱敏/备份RPO≤24h RTO≤4h/注销联动）。
  - P0-6 量化非功能（首字P95≤3s、API≤500ms、预约P95≤1.5s、SSE≤2s、热更新≤60s、可用性≥99.5%、并发≥50TPS、桌面≥1280×720）。
  - P0-7 + §6.1 需求追踪矩阵（需求ID/前置/输入/预期/错误码/阈值/用例ID）。
  - P1：§4.10/§4.11标"架构约束参考"(P1-1)；术语"自建部署+托管API"(P1-2)；推荐问题异步缓存+兜底(P1-3)；RAG质量标准+热更新边界(P1-4/8)；通知Outbox/幂等/撤销(P1-5)；公司名唯一约束公式+手工解除(P1-6)；登录范围决策记录(P1-7)；SSE一致性(DB权威/断线重拉)(P1-9)；成本算术修正含域名180-220/不含130-170(P1-10)。
  - P2：会话术语统一(P2-1)；周末/节假日/调休拆分(P2-2，与P0-2合并)；owner锁已约=自动取消+通知+原因(P2-3)；桌面兼容(P2-4)；隐私占位符发布检查(P2-5)。
  - 设计工件：§8.9角色权限矩阵、§8.10预约状态机、§8.11数据模型(12实体)、§8.12错误码、§8.13 MVP范围(硬性10项+延后项)、§8.14文档拆分建议(PRD/SRS/架构/安全/测试)。
- 仍未写代码。下一步：按评审建议进入 领域模型/接口契约/测试计划 评审。
