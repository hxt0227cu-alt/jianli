# 2026-08-06 工作记忆

## 个人 AI 问答网站 — v2.3 / v1.6 开发准入收口（第三次复审闭环）

### 本轮完成：用例规约 v1.6 剩余同步（任务 #29 收尾）
需求文档 v2.3（序号14）已于前序完成，本轮补齐用例规约 v1.6 残留一致性项：
- UC-12 扩展「邮件兜底」→ 失败通道独立重试 + 经另一通道告警候选人（对齐通知失败矩阵 P0-5）
- UC-14 参与者「邮件/微信助理/飞书推送」→「邮件/飞书推送（外部；微信助理延后）」
- UC-19 后置 `confirm_mail_status` → `mail_delivery_status`（MailDeliveryStatus：queued/accepted/delivered_unknown/failed/bounced），对齐 P0-2 三状态机
- UC-19 扩展 6a「双通道告警候选人（飞书+邮箱）」→「飞书告警候选人 + 后台高优先级告警 + 后台可手动重发」
- UC-19 特殊需求「P95 ≤60s 送达」→「P95 ≤10s 提交 SMTP（不含端到端送达）」（P0-6）；补「会议号变更发更新函（脱敏呈现）」（P2）

### 一致性校验结论（终态）
- 两文档均无「邮件兜底」笼统机制残留；「兜底」仅用于推荐问题/变量/独立重试不退避式互兜底的正确语境。
- 需求文档「动态生成」仅存于：推荐问题异步缓存+兜底（正确 MVP 口径）、延后清单「实时动态生成推荐问题（先用异步缓存+兜底）」遵循"延后特性（先用现状）"模式，无误。
- 知识库热更新文本类 P95≤60s 为 P1-2 正确口径（非确认函口径）。
- 微信通道全文档统一为「延后项，不在 MVP」；混元统一为「延后冗余」；MVP 单 DeepSeek。

### 交付物
- `个人AI问答网站-需求文档初稿.md` = v2.3 开发准入收口版
- `个人AI问答网站-用例规约.md` = v1.6（与 v2.3 同步）

### 下一步待用户决策（编码前分析工件）
评审判定：编码准入暂缓，待《测试计划》完整追踪矩阵 + 领域模型/接口契约评审通过。
建议下一工件二选一：① 领域模型（类图）② 接口契约（REST/SSE/Schema）。

---

## 第三轮 ChatGPT 独立评审收口 + 领域模型 v1.0（续）

### ChatGPT 第三轮评审结论
有条件通过，可进入架构设计；暂不建议编码。判定处 Requirements Baseline，缺三工件：领域模型 / 接口契约 / 测试计划。整改质量评分 8.8/10。

### 评审 P0/P1 对照处理（仅修一致性，不增功能）
- **P0-1**（用例图 S5/UC-05/R4「动态」残留）→ 用例规约改：S5 图标签、UC-05 标题、R4 规则「动态」→「异步缓存」（4 处）。
- **P0-2**（状态机需独立章节）→ 需求文档 §8.10 已是三状态机正式章节，加"唯一规范源"注；评审或漏看 v2.3 已落地。
- **P0-3**（数据模型正式化 ER+表结构）→ 产出独立工件《领域模型设计 v1.0》。
- **P1-1**（热更新 SLA 按文件类型）→ line188 已拆；细化三档：MD/TXT/DOCX/可复制PDF ≤60s、扫描不可复制PDF ≤120s、OCR PDF 异步 ≤5min。
- **P1-2**（黄格语义）→ 颜色表后加注：黄格仅为前端 Session UI 态，不进 SlotStatus、不参与并发、不预占。
- **P1-3**（权限矩阵）→ 纳入《领域模型设计 v1.0》§7 角色权限矩阵（面试官/owner admin/系统任务 × 查看/修改/删除）。

### 新交付物
- `个人AI问答网站-领域模型设计.md` = v1.0：14 实体类图(Mermaid)+ER 图+三状态机规范(引 §8.10)+正式表结构(含 AES/HMAC/部分唯一索引)+权限矩阵+业务不变量映射。

### 下一步（仍编码前）
评审推荐顺序：领域模型(已完成) → 接口契约 v1.0 → 测试计划 v1.0 → 架构评审 → 编码。待用户拍板接口契约或测试计划。

---

## 第四轮工程评审整改包（2026-08-06 下午）

### 评审判定
需求文档有条件通过→v2.3.1；用例规约暂不通过→v1.7；领域模型暂不通过→v1.1；L2 语料清单作延后实验计划；**编码准入不开放**，需完成设计/测试/AI 治理工件。明确顺序：v2.3.1→v1.7→v1.1 整改包优先于接口契约。

### 整改包交付（仅修一致性/闭合缺口，未增产品功能）
1. **PRD v2.3.1**（`个人AI问答网站-需求文档初稿.md`）：
   - 登录方式明确：密码登录；验证码仅用于注册验证与密码找回（不用于登录）
   - §4.10/§4.11/§8.15 顶部加「非规范性内容，不构成 MVP 验收标准，不得直接生成开发任务」醒目标注
   - 新增 §8.4 决策#14：MVP 禁止 LLM 自动写预约（大模型只负责问答）
   - 新增 R14b（owner 强制取消）
   - 清除残留「同步兜底」→飞书同步失败告警、变量兜底→缺失处理；知识库热更新统一 60s SLA→改引分档 SLA
   - §8.10.3 邮件状态升级为通用 DeliveryStatus(queued/sending/succeeded/failed/retry_scheduled/dead_letter) + 邮件/飞书专属元数据拆分；同步 §8.11 数据模型（timestamptz 时段、移除 slot_ids/last_delivery_status、账号域隔离、新增 11 实体、留存字段）
2. **用例规约 v1.7**（`个人AI问答网站-用例规约.md`）：
   - UC-10 描述/基本流统一为原子流程（锁新段→占新→释旧，不先释放）
   - 双通道后置「三端」→「已启用通道分别记录成功/失败」
   - UC-16 热更新 SLA 改引分档 SLA（≤60s/≤120s/异步≤5min）
   - UC-19 确认函补齐会议平台/会议号/起止时间字段与脱敏规则
   - 外部系统表「同步兜底」→「飞书同步失败告警」
   - 「写代码前工件」清单→实时准入检查表（领域模型已出 v1.1）
   - 新增 UC-20(owner强制取消/R14b)/UC-21(通知失败中心)/UC-22(公司去重例外解除)；追溯矩阵补 R14b
3. **领域模型 v1.1**（`个人AI问答网站-领域模型设计.md`，全文重写）：24 实体
   - P0-1 账号域隔离：同物理表按 role 分域；email 全局唯一=单身份单角色；InterviewerProfile/OwnerContactConfig 0..1；新增 AuthSession
   - P0-2 AppointmentSlot 改 timestamptz(start_at/end_at)+UNIQUE+CHECK(30min)
   - P0-3 删除 Appointment.slot_ids[]，占用仅经 AppointmentSlot.appointment_id
   - P0-4 并发落到 Slot 行锁(FOR UPDATE)+事务置 booked+部分唯一索引；明确 Appointment.version 不保护新预约
   - P0-5 CompanyDedupeOverride + Appointment.dedupe_overridden 部分唯一索引排除
   - P0-6 通知状态拆分：通用 DeliveryStatus + EmailDeliveryMetadata + FeishuDeliveryMetadata
   - P0-7 新增 11 实体（AuthSession/EmailVerificationToken/PasswordResetToken/PageAnnouncement/AppointmentEvent/ReminderSchedule/ExternalSyncBinding/CompanyDedupeOverride/RecommendedQuestionCache/CalendarOverride 等）
   - P0-8 留存/软删字段（cancelled_at/completed_at/deleted_at/purge_after/deletion_requested_at）
   - P1 明确 Appointment.company_name_* 为提交时不可变快照；移除 last_delivery_status
4. **AI 编码治理文件**（评审第九章，正式工程工件，已纳入仓库）：
   - `AGENTS.md`（规范优先级/任务驱动/强制停止/防冗余/双角色审查/上下文确认/测试锁定）
   - `PROJECT_STATE.md`（基线/已完成/进行中/风险/下一步/末次测试commit）
   - `docs/baseline.yml`（基线版本+precedence+deferred+硬规则）
   - `tasks/TASK-TEMPLATE.md` + `tasks/TASK-001.md`(样例)
   - `docs/adr/README.md`（ADR 清单，§4.10/§4.11/§8.15 标注为非规范性）

### 关键安全提醒（持续有效）
网易 163 邮箱授权码等密钥**绝不写入任何文档/记忆/配置文件**，仅存运行时环境变量或 Secret Manager。

### 下一步
v1.7/v1.1 待用户/评审通过后，按序进入：SRS v1.0 → UI 线框 → 架构与 ADR → 安全设计 → 接口契约(OpenAPI/SSE) → 测试计划 → 开发准入评审 → 编码。

---

## 第五轮治理精简（2026-08-06 晚）— 文档单一规范源整改

### 用户核心判定
10 个文件本身不算多，但"同一事实在 3~4 个文件各写一遍"导致维护漂移 + AI 注意力稀释。立即调整为：活跃文件压到 7 个；L2 / ADR README / 样例 TASK 移出默认上下文；baseline.yml 成版本+状态唯一规范源；PROJECT_STATE 去版本表；AGENTS 不再复制 precedence/deferred/hard rules；下一步 SRS 吸收用例规约（不长期双向维护）；领域模型回到 MVP 最小实现模型。

### 文件结构重组（物理移动）
- `个人AI问答网站-需求文档初稿.md` → `docs/requirements/PRD.md`
- `个人AI问答网站-用例规约.md` → `docs/requirements/use-cases.md`
- `个人AI问答网站-领域模型设计.md` → `docs/design/domain-model.md`
- `个人AI问答网站-L2人格语料收集清单.md` → `docs/experiments/deferred/l2-persona-training.md`（非 MVP，AI 默认不读）
- `tasks/TASK-001.md` → `tasks/examples/TASK-EXAMPLE.md`（仅样例，非已批准任务）
- 活跃 7：`AGENTS.md` `PROJECT_STATE.md` `docs/baseline.yml` `docs/requirements/PRD.md` `docs/requirements/use-cases.md` `docs/design/domain-model.md` `tasks/TASK-TEMPLATE.md`
- 非活跃：`docs/adr/README.md` `docs/experiments/deferred/l2-persona-training.md` `tasks/examples/TASK-EXAMPLE.md`

### baseline.yml 重构（唯一规范源）
- 改用 `artifacts:` 嵌套（version + status 成对），补 `security: "0.0"`（修复 development_gate 引用缺失）
- 保留 precedence / deferred / mvp_hard_rules
- 新增 active_context / inactive_context 清单 + development_gate

### PROJECT_STATE.md 去重
- 删除版本表与路线图重复内容；仅留 当前阶段/当前任务/本周阻塞/下一步/末次测试commit；版本一律指向 baseline.yml

### AGENTS.md 两层读取 + 去重
- 每次会话强制读：AGENTS / baseline.yml / PROJECT_STATE / 当前 TASK
- 按任务按需读：任务单精确列出 PRD/用例/SRS/domain §x.x、ADR、OpenAPI opId、TC-xxx；禁止全量加载规格
- 删除 duplicated precedence/deferred/hard rules，改引用 baseline.yml

### 领域模型 v1.1 → MVP 最小实现模型（修复过度建模 + 清单不一致）
- 实体计数修正：原文标"24 个"但类图含 AuditLog 实际超 24；且回复声称含 `RecommendedQuestionCache` 实际缺失
- 现明确：**MVP 建表实体 19 个**（含新增的 RecommendedQuestionCache 最小表）
- **7 项能力合并实现（不独立建表）**：Email/Feishu 元数据→NotificationDelivery.channel_metadata JSONB；ExternalSyncBinding→配置中心单例+AuditLog；CalendarOverride→OwnerBlock.type=holiday_override；CompanyDedupeOverride→Appointment.dedupe_override_* + AuditLog；AppointmentEvent→AuditLog；ReminderSchedule→NotificationEvent(type=reminder,scheduled_at)
- 同步 PRD §8.10.3/§8.11、用例 UC-22 后置、L2 文件引用，消除与"领域模型为 DB 唯一规范源"的冲突

### 关键安全提醒（持续有效）
网易 163 邮箱授权码等密钥绝不写入文档/记忆/配置文件，仅存运行时环境变量或 Secret Manager。

---

## 第六轮复评 · v1.1.2 最终开发前一致性整改包（收口）

### 用户指令边界（恒定）
仅做一次性最终一致性整改：PRD v2.3.3 / 用例 v1.7.2 / 领域模型 v1.1.2 / AI 治理 v1.0.1 / 删 ADR README / baseline 同步。**禁令**：不新增功能、不启动 SRS、不启动接口设计、不新增 Agent/AI Infra、不增未来扩展表、不创建开发任务或代码。

### 交付物（全部落盘，版本一致，已 Grep 互检）
- **PRD v2.3.3**：P0-1 认证方式改「密码登录+注册邮箱验证+邮箱验证码找回」；P0-2 R4 删「实时生成」冲突，统一后台异步缓存+页面只读+缺失固定兜底；P0-3 §8.11 字段明细表整节删除→「数据边界」仅列实体名并指向领域模型为唯一规范源；P0-7 `DeliveryAttempt` 幽灵实体全文改 `NotificationDelivery` 尝试记录。
- **用例 v1.7.2**：P0-4 UC-20 取消后三格 SSE 刷新 `owner_locked`（红色不可约，非绿）；P0-5 新增 UC-23 后台只读应急视图、R14a 追溯由 UC-11→UC-23；P0-7 UC-21 重试改新建 `NotificationDelivery`；场景 S1–S19。
- **领域模型 v1.1.2**：P0-6 `CompanyBookingException` 补 `revoked_at/revoked_by`+`FOR UPDATE`+`uq_appointment_exception`（未消费可撤销/已消费不能靠撤销删预约）；P0-7 `NotificationDelivery` 补 `UNIQUE(event_id,channel,event_version,attempt_no)`；P0-8 `RecommendedQuestionCache` 简化 `page_key/questions_json/generated_at/invalidated_at`+`UNIQUE(page_key) WHERE invalidated_at IS NULL`，取消全局版本表；`AvailabilityOverride` 补 NOT NULL+CHECK+`created_by` FK；并发三格连续性服务端校验；**新增 §1.2 逻辑领域模型 vs 物理 Schema 分离声明**（逻辑 SRS 前通过、物理 ADR 接受后冻结；SQL 片段为语义示意非最终 DDL）。
- **AI 治理 v1.0.1**：`baseline.yml`（prd/uc/dm=review、ai_governance=approved；`precedence` 加 `accepted_adr`+`security_design` 置于 `domain_model` 前；`mandatory_context` 拆 `always`/`when_modifying_repo`）；`TASK-TEMPLATE.md` 停止条件统一「列明才继续、未列明即停」+新增「交付证据」章节；`AGENTS.md` §0/§2/§6 同口径。
- **参考文件 P0-10**：`agent-engineering-frameworks.md` 拆 A 原框架/B 适用性矩阵/C ADR 输入；**删除全部「预约 Agent 写面试表」映射**，新增 B.2「Agent 不得写业务数据」硬规则。
- **文件删减**：`docs/adr/README.md` 移出仓库→归档 `.workbuddy/archive/2026-08-06/adr-README.md`；规范文件 10→**9**；`l2-persona-training.md` 去 PRD 版本硬绑定。

### 关键决策（后续复用）
- 逻辑领域模型与物理 DDL 分离：领域模型文件 SQL 仅为语义示意，写迁移须等 ADR accepted。
- `mandatory_context` 分层：纯咨询/评审无需 TASK，但无 TASK 不得写仓库。
- 网易 163 授权码等密钥绝不入文档/记忆/配置（持续有效）。

### 状态
v1.1.2 整改包完成，第六轮复评结论已下达：功能与领域设计通过、文档治理有条件通过；修完 5 处一致性残留并去重 PROJECT_STATE 后，PRD/用例/领域模型/AI 治理基线均置 `approved`，可进入 SRS，不开启 v1.1.3 大轮次。仍未写任何代码。

---

## 第六轮复评收尾补丁（"复评收尾补丁"，非 v1.1.3）

### 用户最终判定
功能与领域设计通过；文档治理有条件通过；5 处一致性残留修完 + PROJECT_STATE 去重后，baseline 三件套改 approved，直接进入 SRS v1.0，不再第七轮全量整改。编码仍不开放（development_gate 仍要求 SRS/UI/架构/安全/OpenAPI/测试计划全部 approved）。

### 补丁内容（保持版本号不变，未扩需求/未重写领域模型）
1. **Fix1 PRD 版本记录**：「第六轮一致性整改通过，可进入 SRS；编码准入不开放」→「整改包完成，提交第六轮复评；编码准入未开放」（评审状态只由 baseline 维护）。
2. **Fix2 领域模型头部**：删「第五轮…待复评」评审状态，改「当前版本与评审状态见 docs/baseline.yml。编码准入由 baseline development_gate 决定」。
3. **Fix3 用例规约头部**：删「§8.2 待确认项全部关闭后方可进架构/接口/编码」，改「评审通过后可进 SRS/UI/架构设计；外部资源待确认项不阻塞设计，但关闭前不得完成外部集成验收或发布上线；编码准入以 baseline development_gate 为准」。
4. **Fix4 UC-23**：删「或作为日常飞书完整视图的补充」，改「飞书不可用或飞书同步异常时」，与 R14a 对齐，避免扩大解密场景。
5. **Fix5 PRD 通知/模型措辞**：飞书/邮件失败→失败通道独立重试并告警，不切换其他通道承担其职责；同步失败同口径；MODEL_UNAVAILABLE→返回明确的服务暂不可用提示，不调用第二模型、不生成无依据回答。保留「固定兜底问题」（推荐问题，不涉及通知通道/第二模型）；保留「混元 Fallback 延后」（MVP 单 DeepSeek 设计决策）。
6. **Fix6 PROJECT_STATE 去重**：删「当前任务」中复制的版本号+review 状态；改恒定读取为「AGENTS→baseline→PROJECT_STATE；仅改仓库时追加 TASK」；当前阶段同步反映复评通过+approved+进入 SRS。
7. **Fix7 baseline**：prd/use_cases/domain_model 由 review→approved（ai_governance 已 approved）。

### 状态
复评收尾补丁完成，三件套+AI 治理均 approved；编码准入仍由 development_gate 门禁（SRS/UI/架构/安全/OpenAPI/测试计划 pending）。下一步：按用户授权进入 SRS v1.0（本回合按"只做复评收尾补丁"指令未启动 SRS 正文，待用户确认后开始）。

---

## 第六轮复评收尾补丁·验收 + 治理微补丁（SRS 前最后 4 项残留）

### 用户验收结论
核心业务/领域模型通过、通知/单模型降级/UC-23 边界/Agent 隔离通过、9 文件合理、baseline 单一状态源暂未完全实现。不新开 v1.1.3、不第七轮全量评审；修 4 项治理残留 + 创建正式 SRS TASK 后可进 SRS。编码仍未开放（正确）。

### 关键纠正（用户点名"太早"）
上一轮我"内部建任务清单后直接改文件"违反 AGENTS「无 TASK 不得写仓库」。本轮改为：**先创建正式 tasks/TASK-SRS-001.md 并展示，再以它为唯一范围约束执行补丁**，不提前写 SRS 正文。

### 微补丁（保持版本号不变，未扩需求/未重写领域模型）
1. **P0-1 用例规约**：删「use_cases 状态为 review」硬编码，改「当前版本与评审状态见 docs/baseline.yml」，不再复制 approved（消除与 baseline 漂移）。
2. **P0-2 PRD 尾部**：删旧「v2.3 已完成第三轮复审…可进入用例规约/接口契约/测试计划」现行正文，改指向 baseline + PROJECT_STATE，不重复维护实时门禁。
3. **P0-3 baseline precedence**：`accepted_adr`→`architecture`、`security_design`→`security`（用现有工件键，消除机器标识漂移）；注释「仅 accepted ADR 产生约束」。未新增 adr_registry。
4. **P1-1 PROJECT_STATE 去重**：删重复 approved 表述与「v1.1.2 完成」硬编码；「最后更新」只留日期+事件；当前阶段/任务改指向 baseline。
5. **P1-2 PRD §8.11**：「domain-model.md（v1.1.2）为唯一规范源」→「当前在 baseline.yml 中批准的版本为唯一规范源」（防未来版本过期漂移）。
6. **P1 TASK 模板**：适用范围扩为「无任务单不得写入仓库，含文档/设计/测试/代码」；新增「## 任务类型」(documentation/design/implementation/test/migration)。

### baseline 严格做法
按用户「最严格做法」：微补丁进行中先将 use_cases / ai_governance 置 `review`（避免过早 approved），全部补丁落盘后复评置 `approved`；domain_model 保持 approved。最终四件套均 approved，编码门禁未开放。

### 新建工件
- `tasks/TASK-SRS-001.md`：SRS v1.0 生成 + 治理微补丁的唯一范围约束（documentation 类型；允许修改列明 PRD/用例/baseline/PROJECT_STATE/TASK-TEMPLATE/SRS/本任务单；禁止改业务需求与领域模型不变量/代码/迁移/OpenAPI/Agent 参考）。

### 状态
4 项残留 + TASK 模板 + 正式 SRS TASK 均完成。全仓 Grep 无 `待复评`/`accepted_adr`/`security_design`/旧 v2.3 准入/硬编码 review 残留。下一步：用户确认 TASK 后，再起草 SRS v1.0 大纲与正文（未在此回合写 SRS 正文）。

---

## 第六轮复评·最后一次机械补丁（TASK 启动前补丁 P0 收口 + 4 治理残留）

### 用户最终指令（不再碰业务、不升版本、不第七轮评审）
TASK 启动前补丁已落（R1–R26/领域模型章节号/AGENTS §1/SRS 状态流转/repository_not_initialized/交付证据清空/precedence 注释/PROJECT_STATE 冻结时机/ai_governance 临时 review）。本轮补机械残留：

### 本次落盘（全部为治理措辞一致性，未触碰业务/领域模型/代码）
1. **TASK-SRS-001 基线行**：删「（取自 docs/baseline.yml，均已 approved）」→「（取自 docs/baseline.yml；版本如下，评审状态以 baseline.yml 为准）」。
2. **TASK-SRS-001 PRD 引用重写（P0 错位修正）**：原误写「§1 范围/§3 全部功能需求/§4 非功能…」→ 真实 PRD v2.3.3 章节：§1 项目背景 / §2 需求目标 R1–R26 / §3 业务场景 / §4 功能需求 / §5 非功能需求 / §6 验收标准 / §8.2–§8.4 / §8.6 / §8.7 / §8.9–§8.13；明确不读 §8.1/§8.5/§8.8/§8.14。
3. **TASK-SRS-001 回滚方法**：「git 历史回退」→「修改前保留原文件副本/patch，初始化 Git 后使用 git restore/revert」（对齐 repository_not_initialized）。
4. **TASK-SRS-001 允许修改路径扩围**：AGENTS §0 上下文读取句、use-cases 冻结句、TASK 模板 commit 指引 三处已列明可改。
5. **AGENTS §0**：「SRS 产出后改为」→「SRS approved 后改为」。
6. **use-cases.md 冻结句**：「SRS 产出并吸收本规约后，本文件冻结」→ 对齐 PROJECT_STATE：「SRS v1.0 经评审 approved，且完成需求—用例—SRS 追踪检查后，本文件冻结」。
7. **TASK-TEMPLATE commit 指引**：「如尚未编码写无」→「仓库未初始化 Git 时写 repository_not_initialized；初始化后回填真实 commit hash」。
8. **baseline.ai_governance**：Grep 校验无残留「均 approved/SRS 产出后/尚未编码写无/git 历史回退/旧 PRD 引用」后，由 review 恢复 approved。

### 校验
全仓 Grep 通过：无 `均 approved` / `SRS 产出后` / `尚未编码写无` / `git 历史回退` 残留；旧 PRD 引用「§1 范围/§3 全部功能需求」已消除（仅剩领域模型 §1 范围为合法）。最终 baseline 状态：prd/use_cases/domain_model/ai_governance=approved；srs/ui_wireframe/architecture/security/openapi/test_plan=pending（正确）。

### 状态
最后一次机械补丁完成，治理已对齐 baseline 单一规范源。用户确认：「清掉残留即可开始 SRS v1.0，不需要第七轮业务评审。」下一步待用户确认后，按 TASK-SRS-001 允许范围起草 SRS v1.0（先大纲+裁剪范围确认，再落正文；SRS 吸收用例规约）。编码准入仍由 development_gate 门禁（未全 approved 不得编码）。
