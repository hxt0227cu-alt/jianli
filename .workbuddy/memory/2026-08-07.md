# 2026-08-07 工作记忆

## TASK-SRS-001 启动前补丁（第六轮复评收尾·治理微补丁验收后）
- 用户验收上一轮治理微补丁通过，但 TASK-SRS-001 本身有 2 项 P0（漏 R15–R26、领域模型章节号错）+ 状态流转/交付证据/AGENTS 对齐问题，判定暂不可写 SRS。
- 本轮按第九节 9 项最小修订（不升版本、不重开业务评审、不写 SRS 正文），仅改 4 文件：
  - `tasks/TASK-SRS-001.md`：精确规范引用 R1–R14b → R1–R26（含 R14a/R14b）+ §4/§8.10/§8.12；领域模型引用改 §1/§2/§5–§9；允许路径加 AGENTS.md（max_files:8 仍够）；加 SRS pending→review→approved 流转（WorkBuddy 不得自标 approved）；基线 commit 填 `repository_not_initialized`（仓库未初始化 Git）；交付证据清空为回填占位，治理 Grep 移至阶段性证据。
  - `AGENTS.md` §1 改"无任务单不得修改仓库"（文档/设计/测试/配置/迁移/代码），与 TASK 模板一致。
  - `baseline.yml`：precedence 加注释"只有 approved 工件参与 precedence；pending/review 不覆盖已批准工件"。
  - `PROJECT_STATE.md`：用例冻结时机改"SRS v1.0 approved 且完成需求—用例—SRS 追踪检查后"。
  - `ai_governance` 临时置 `review`（因修正 AGENTS 属其范畴），待用户确认后恢复 `approved`；`domain_model` 保持 `approved`。
- 全程未写 SRS 正文、未新建开发任务、未碰代码。
- 下一步：用户确认 TASK + AGENTS 修正 → 我恢复 `ai_governance: approved` → 再起草 SRS v1.0（先大纲+裁剪范围确认，再落正文；SRS 吸收用例规约）。
