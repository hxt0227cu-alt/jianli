# 2026-08-08 工作记忆（纯连续性记录，非项目治理闭环）

## 领域模型 v1.1.3 批准 + SRS impact review 收口（用户明确授权）

用户 2026-08-08 明确批准 domain_model v1.1.3，并授权执行治理 ⑥⑦ 步骤（**不代签 SRS 批准**）。

### 提交链（向前修正，历史未重写）
- `f64b6de` — baseline `domain_model.status: review → approved`（批准锚点）
- `e216d01` — TASK-DM-001 关闭证据补全（verified_commit=f64b6de）+ 关闭结论 + 状态已关闭（Closed）
- `ba9495d` — SRS impact review 收口：baseline.srs.based_on.domain_model 1.1.2→1.1.3；TASK-SRS-001 spec_sync dirty→clean；PROJECT_STATE 同步当前态

### 当前基线状态（baseline.yml）
- domain_model: 1.1.3 / **approved**
- srs: 1.0 / **review**（未代签；based_on.domain_model=1.1.3；spec_sync=clean；verified_commit 仍 <回填> 待用户批准 SRS 后生成）
- ui_wireframe: 0.0 / pending（TASK-UI-001 + ui-wireframe.md 仍标记基线无效/不得评审，冻结）

### 关键纪律（已落实，持续有效）
- 「完成后可以批准」≠「现在批准」；状态推进（approved）必须由用户显式改 baseline，AI 不代签。
- 三者一致性：每次产物状态推进须检查 baseline + PROJECT_STATE + current TASK。本轮 PROJECT_STATE 已同步（域模型已批准 / SRS 仍 review / UI 冻结）。
- .workbuddy 是 WorkBuddy 自身工作目录，非项目治理证据；记忆写入不计入项目闭环。
- 密码哈希算法（BCrypt vs Argon2id）裁定仍留《安全设计》ADR，不在领域模型/SRS 阶段预选。

### 下一步（待用户操作）
- 用户独立评审批准 SRS → 改 baseline srs.status: approved → 补 TASK-SRS-001 verified_commit + 关闭 → 重做 UI 线框 impact review 决定是否沿用 → 进架构/ADR。
- 不得继续架构/API/测试计划/编码阶段，直到 development_gate 全 10 项 approved。

## P0 评审回退（用户独立评审 domain_model 1.1.3 后指出算法仍被锁定）

用户评审发现：domain_model 虽声明"不预选算法"，但 5 处仍指向 Argon2id（含 line 7 整改说明段、line 19/425 正文/表、line 95/320 图模型仅写 Argon2id 无"待裁定"说明），与 PRD BCrypt、SRS"不预选"冲突。实为已批准工件的实质缺陷。

### 修正（向前修正，历史未重写）
- `236d302` — domain-model 5 处 Argon2id 全部改算法中性：正文/表写"存密码哈希不存明文，算法待 Security ADR，不预选 Argon2id/BCrypt"；图模型用 `password_hash string "algorithm pending Security ADR"`。
- `36ae776` — 治理回退：baseline `domain_model.status: approved→review`（编辑已批准工件须重审）；TASK-DM-001 重新开启（状态 Reopened，关闭结论作废）；验收结论去"当前按 Argon2id 设计"；关闭条件改为非循环（SRS based_on/impact review 归下游 TASK-SRS-001，不构成本任务关闭前提）；Grep 证据由"全仓无 purge_before"改"活动规范正文无残留字段"（任务历史引用不计）；PROJECT_STATE 同步；TASK-SRS-001 line 111 同措修正（顺手，同一准确性原则）。

### 当前基线状态（更新）
- domain_model: 1.1.3 / **review**（重新开启修正，待用户重新评审批准；版本号维持 1.1.3 未升版）
- srs: 1.0 / **review**（based_on.domain_model=1.1.3 未变、与 baseline 一致；TASK-SRS-001 spec_sync=clean 仍有效，无需改 SRS）
- ui_wireframe: 0.0 / pending（冻结）

### 用户反馈两点校正（重要）
- 用户质疑的"baseline line 20 仍是 1.1.2"经核实为**过期状态**：当前 baseline.srs.based_on.domain_model 已是 1.1.3（上一轮 ba9495d 已同步），与 SRS line 4 一致，无不一致，无需改动。
- .workbuddy 非项目治理证据，记忆写入不计入闭环（重申）。

### 下一步
- 用户重新评审 domain_model 1.1.3（算法中性后）→ 改 baseline domain_model.status: approved → 补 TASK-DM-001 verified_commit + 关任务 → 再评 SRS。

## 版本完整性修正：算法中性化改由 v1.1.4 承载（用户否决"复用 1.1.3"方案）

用户指出上一轮做法本身错误：**v1.1.3 已在 `f64b6de` 获得正式批准，`236d302` 又改了其规范正文——同一版本号不能对应批准前后两份不同内容**；也不得以"避免 SRS based_on 连锁变更"为由复用已批准版本号。上一轮把 1.1.3 从 approved 回退 review + 重开 TASK-DM-001 的做法，等于把已发生的批准描述成从未发生。

### 修正（向前修正，历史未重写）
- `ac1745a` — ① TASK-DM-001 恢复**历史已关闭**（Closed，锚点 f64b6de 保留为旧版 1.1.3 真实批准锚点，注明已被 v1.1.4 取代、本任务不重开）；② 新建 **TASK-DM-002** 承载算法中性化；③ domain-model 升版 **v1.1.4**（标题 L1 / 整改记录 L8 / 页脚 L629 三处一致；L7 保留 1.1.3 历史整改记录并如实记其缺陷）；④ baseline `domain_model = 1.1.4 / review`；⑤ 新增**冲突升级条款**（§1 + §6.1 交叉引用）：Security ADR 与 PRD §8.7 BCrypt 不一致 → 必须触发规范影响/变更评审，**不得直接实现**；⑥ `srs.based_on.domain_model` **有意保留 1.1.3** 使机器门禁报 needs impact check；TASK-SRS-001 spec_sync clean→**dirty**；⑦ PROJECT_STATE 同步。
- `e31ad11` — TASK-DM-002 候选交付证据回填（7 项 Grep 一致性结论）；`verified_commit` **留空**，不得复用 f64b6de。

### 当前基线状态
- domain_model: **1.1.4 / review**（待用户独立批准，AI 不代签）
- srs: 1.0 / review；based_on.domain_model=**1.1.3**（有意滞后）；TASK-SRS-001 spec_sync=**dirty**
- ui_wireframe: pending（TASK-UI-001 + ui-wireframe.md 继续冻结）

### 新增治理纪律（跨会话恒成立）
1. **已批准工件不得原地改内容**：批准是版本级事实。改已批准工件的规范正文 → 必须**升新版本号**承载，不得回退同版本号状态、不得复用版本号。
2. **不得为减少下游连锁变更而复用版本号**——"连锁成本"不是版本完整性的豁免理由。
3. 历史批准锚点必须保留并如实标注"已被 vX 取代"，**不得描述成从未发生**。
4. 上游升版后，下游 based_on 应**有意滞后**，让机器门禁自然报 needs impact check，而不是抢先同步。
5. impact review 结论若涉及下游文字过期（如 SRS §6.3 记 Argon2id），**不得记 none**，应记"需文字同步、不改变用户可观察行为"。

### 下一步（等待用户）
- 用户批准 **domain_model v1.1.4** → AI 生成**独立**批准锚点（禁用 f64b6de）+ 关闭 TASK-DM-002 → 执行 SRS impact review（based_on→1.1.4 + 修 SRS §6.3 过期 Argon2id 描述，结论≠none）→ spec_sync 转 clean → 用户独立批准 SRS。UI 继续冻结。

## 第二轮复核：TASK-DM-002 关闭门禁修正（实质修正通过，仍不批准 v1.1.4）

用户复核 v1.1.4 实质内容基本通过，但发现 3 类**关闭门禁**问题：① TASK-DM-002 允许路径/change_budget 只写 3 文件，`ac1745a` 实际改 6 文件（账目不实）；② 冲突升级条款"由评审决定更新 PRD 或采纳 ADR"允许规范继续冲突并存；③ TASK-DM-001 历史结论把"1.1.3 已不预选算法"写成成立，实为反写。

### 修正（向前修正，历史未重写）
- `8135257` — ① 允许路径补列 3 治理文件（TASK-DM-001.md / TASK-SRS-001.md / PROJECT_STATE.md，授权来源＝用户十步纠偏第 1/5/6 步；非越权但登记不实）；② change_budget max_files 3→6、文件清单与实际值改 6、书面记录"任务范围账目偏差"、「是否偏离 TASK」否→**是（账目层面）**；③ 冲突升级条款收紧（L8/§1/§6.1 三处）：ADR 拟选算法与 PRD §8.7 不一致 → **须先经 Change Request 更新并批准受影响规范（PRD+SRS），规范同步完成前不得实现**；④ TASK-DM-001 五处追记历史结论限定（f64b6de 快照仍含 5 处 Argon2id，当时"不预选"判断不完整，中性化属 v1.1.4）；⑤ 关闭条件新增第 3 项（账目四处须一致为 6）。
- `deb3692` / `a9eb262` — 证据链回填 8135257 + 消除自引用占位符（纯回填提交以 git log 为准）。

### 新增治理纪律
6. **任务单必须如实登记实际改动范围**：即使改动本身有用户授权，允许路径/change_budget 未列明也构成"任务范围账目偏差"，不得写"无偏离"。授权 ≠ 登记。
7. **冲突升级条款不得允许规范并存冲突**：ADR 与已批规范冲突时，必须"先改规范并批准，再实现"，不能写成"由评审决定更新 A 或采纳 B"。
8. **不得把后续版本成果反写进历史版本**：批准时点快照有什么缺陷就如实记什么，历史评审结论可被限定为"不完整"，但不得追认为已完成。

### 状态（未变）
domain_model=**1.1.4/review**（未代签）；srs.based_on.domain_model=1.1.3（有意滞后）；TASK-SRS-001 spec_sync=dirty；TASK-DM-002 verified_commit 留空；UI 冻结。

## TASK-DM-002 关闭（用户批准 v1.1.4 后，按 7 步指令第 5/6 步收口）

用户 2026-08-08 明确批准「我批准领域模型 v1.1.4 的内容」，并下达 7 步指令（**不得代签 SRS 批准**）。步骤 1–4 已提交（`82d0ab2` 门禁+顺序修正 / `f537296` v1.1.4 独立批准锚点 / `d166992` SRS impact review）；步骤 5–6 收口完成（关闭提交 `94bedb5`）。

### 提交链（本轮收口，向前修正、历史未重写）
- `82d0ab2` — TASK-DM-002 关闭门禁对齐 TASK-TEMPLATE（补 spec_sync=clean 条件、作废「下游不构成关闭条件」免责、新增「状态推进授权」节）+ PROJECT_STATE 门禁顺序纠正。
- `f537296` — domain_model v1.1.4 独立批准锚点：baseline `domain_model.status: review→approved`（单一用途提交，未复用 f64b6de）。
- `d166992` — TASK-SRS-001 SRS impact review：`srs.based_on.domain_model` 1.1.3→1.1.4；SRS §6.3 过期 Argon2id 描述修正 + 四处版本引用同步；spec_sync dirty→clean。
- `94bedb5` — TASK-DM-002 关闭收口：补「验证结果/关闭结论」四条件复核、状态标 Closed、verified_commit=`f537296`；PROJECT_STATE 同步（domain_model=approved、TASK-DM-002=Closed、TASK-SRS-001 仍开且 spec_sync=clean/SRS review、UI 冻结）。

### 当前基线状态（本轮收口后）
- domain_model: **1.1.4 / approved**（用户批准，锚点 f537296，不复用 f64b6de）
- srs: 1.0 / **review**（未代签；based_on.domain_model=**1.1.4**；TASK-SRS-001 spec_sync=**clean**；verified_commit 仍 <回填> 待用户批准 SRS 后生成）
- ui_wireframe: 0.0 / pending（TASK-UI-001 + ui-wireframe.md 仍冻结，不得评审）

### 纪律落实（持续有效）
- 关闭门禁：TASK-DM-002 在 `spec_sync=dirty` 时不得关闭；须待下游 SRS impact review 完成且 TASK-SRS-001 spec_sync=clean 后，本任务 spec_sync 方可转 clean 再关闭（已验证执行）。
- SRS 批准权在用户：本任务关闭**不代签** SRS approved；SRS 仍为 review，待用户独立评审。
- development_gate 仍待 srs/ui_wireframe/architecture/security/openapi/test_plan 等 approved 后方可编码。

### 下一步（待用户操作）
- 用户独立评审批准 SRS → 改 baseline srs.status: approved → 补 TASK-SRS-001 verified_commit + 关闭 → UI 线框重新 impact review → 架构/ADR → 安全设计 → OpenAPI/SSE → 测试计划 → 开发准入评审 → 编码。
