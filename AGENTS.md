# AGENTS.md — AI 编码协作规范（强制约束）

> 本文件是 AI 参与本仓库开发时的**强制行为约束**，优先级高于模型自身"想帮忙"的倾向。
> 适用对象：实现 AI（写代码）、审查 AI（检查实现）、评审 AI。
> **规范优先级、当前版本、MVP 延后项、硬规则、上下文清单、编码准入门禁——一律以 `docs/baseline.yml` 为唯一规范源。本文件不复制这些内容，改一处即漂移。**

---

## 交互输出语言（SSOT，唯一规范源）

- 与用户的默认交互语言为简体中文（zh-CN）。
- 除非用户在当前消息中明确要求使用其他语言，否则解释、进度汇报、评审结论、任务总结和提问均使用简体中文。
- 代码、命令、文件路径、Git commit、API 字段、配置键、标准技术术语可保留英文原文，例如 `verified_commit`、`spec_sync`、`based_on`、`OpenAPI`。
- 用户消息中包含英文技术材料、英文日志或英文引用，不视为用户要求切换回复语言。
- 不得仅因为最近上下文、工具输出、代码注释或规范中英文术语较多，就自行切换整段回复语言。
- 优先级：**用户明确要求 > 本规则默认 zh-CN > 最近上下文语言**（即最近上下文语言不得自动覆盖默认中文）。

---

## 0. 两层读取模型（控制上下文体积，防注意力稀释）

**每次会话恒定必读（`baseline.yml` → `mandatory_context_always`）：**
- `AGENTS.md`（本文件）
- `docs/baseline.yml`（版本/状态/优先级/延后项/硬规则唯一源）
- `PROJECT_STATE.md`（当前阶段/任务/阻塞/下一步）

**仅在要改动仓库时追加必读（`mandatory_context_when_modifying_repo`）：**
- 当前 `tasks/TASK-*.md`（任务范围唯一约束）
- 纯咨询 / 评审 / 解释类会话不需要 TASK 文件；但**没有 TASK 就不得写入仓库**。

**按任务按需读取（不全量加载规格）：**
- 任务单必须精确列出需引用的章节，AI 只读这些章节：
  - `docs/requirements/PRD.md §x.x`
  - `docs/requirements/use-cases.md §x.x`（SRS approved 后改为 `SRS §x.x`）
  - `docs/design/domain-model.md §x.x`
  - `docs/adr/ADR-00x`（如有）
  - `openapi` operationId（接口契约产出后）
  - 测试用例 `TC-xxx`
- **禁止**为"覆盖全面"而全量加载 PRD/用例/领域模型/架构/OpenAPI/测试计划。
- 非活跃上下文（见 `docs/baseline.yml` `inactive_context`：`docs/experiments/deferred/l2-persona-training.md`、`docs/references/agent-engineering-frameworks.md`）默认不读；`agent-engineering-frameworks.md` 仅作 ADR 输入与完整性检查，**不得据此生成开发任务**。

冲突时**立即停止并报告**，不得自行选版本或悄悄改需求来"让实现通过"。

---

## 1. 任务驱动（无任务单不得修改仓库）

- 每个仓库变更任务必须有独立 `tasks/TASK-XXX.md`，模板见 `tasks/TASK-TEMPLATE.md`。
- 任务单明确：**基线版本 / 精确规范引用 / 目标 / 非目标 / 允许修改路径 / 禁止修改路径 / 已批准 DB·API·依赖变更 / 功能·安全·性能验收 / 变更预算 / 回滚 / 交付证据**。
- 没有任务单，不允许修改任何仓库文件，包括文档、设计、测试、配置、迁移和代码。
- 规格变更、测试期望变更、业务实现**不得在同一个 PR 中一起完成**（流程：Change Request → 更新规格 → 规格评审 → 新任务单 → 写代码）。

---

## 2. 强制停止条件（Stop & Report）

**唯一判定口径（与 `tasks/TASK-TEMPLATE.md` 一致）：看变更是否已在当前 TASK 的「已批准的 DB / API / 依赖变更」章节中逐项列明，而不是看变更类型本身。**

- **可继续**：变更已在该章节列明，**且**其依据工件在 `docs/baseline.yml` 中为 `approved`（ADR 为 `accepted`）。
- **必须立即停止并报告**：出现任何**未在该章节列明**的变化，包括但不限于
  - 需求与接口冲突；
  - 需要新增外部依赖（npm/pip 包）；
  - 需要新增数据库表、字段、索引或迁移；
  - 需要修改公开 API / SSE 事件 / 契约字段；
  - 需要改变加密 / 密钥 / 鉴权策略；
  - 需要实现任务范围外的功能（含 `deferred` 延后项）；
  - 发现现有代码与领域模型不一致。
- **与列明与否无关的硬停**：冻结验收测试失败；超出 `change_budget`。

**变更预算（change_budget）**：文件数 / 代码行数阈值**不写死为全项目硬停**，由每个 TASK 在 `change_budget` 中声明（`max_files` / `expected_prod_lines` / `expected_test_lines`）。超过预算即**拆任务**，不直接继续。

---

## 3. 防止冗余与过度设计

- 创建模块前先搜索同类实现（repo grep）。
- 同一逻辑未出现 **3 次**前，不创建通用抽象。
- 禁止为单一接口创建 `BaseManager` / `AbstractFactory` 等抽象。
- 禁止未经 ADR 新增框架、中间件、外部服务（含 LangGraph / MCP / Mem0 / 微服务 / Agent 自动预约）。
- 禁止复制后微调，应提取已有公共函数。
- 禁止保留未被使用的"未来扩展"代码 / 空壳类 / 预留字段。
- **延后功能不得创建空壳类、数据库字段或接口**（见 `docs/baseline.yml` `deferred` 列表）。
- 大模型**只负责问答**（RAG + 人格层 L1），**不得自动调用预约写入工具**（PRD 决策#14）。

---

## 4. 双角色审查（实现 AI + 审查 AI）

- 实现 AI 只负责 `tasks/TASK-*.md` 范围内的代码。
- 独立审查 AI 只检查：越界 / 新依赖 / 重复实现 / 违背领域模型 / 遗漏异常路径 / 测试真实覆盖 / 安全边界变化。
- 以下变更**必须人审批**：数据库迁移、鉴权、加密、外部通知、Prompt 与工具权限、基础设施。

---

## 5. 会话开始上下文确认（必输出）

编码前 AI 必须先输出：
```
基线：PRD <ver> / 用例规约 <ver> / 领域模型 <ver>（取自 baseline.yml）
任务：TASK-XXX
目标：<一句话>
非目标：<明确排除项，如飞书/邮件/Agent>
允许修改：<模块清单>
预计变更：<文件数 / 行数，对照 TASK.change_budget>
验收测试：<TC 编号>
输出语言：简体中文（除非用户当前明确要求其他语言）
```
内容不正确时立即终止会话。

---

## 6. 会话结束证据（必输出）

任务完成时 AI 必须输出（不得只说"已完成"）：
```
修改文件：<清单>
需求覆盖：<Rxx / TASK-xxx>
测试命令及结果：<命令> → <pass/fail 数>
lint / typecheck：<结果>
数据库迁移结果：<up/down 是否通过>
未解决风险：<或"无">
是否偏离 TASK：<否 / 偏离项>
建议审查重点：<具体>
```
同时必须回填当前 `tasks/TASK-XXX.md` 的「**交付证据**」章节（commit/PR、测试结果、迁移验证、截图或接口响应、变更预算实际值）；**该章节未填写完整，任务不得关闭**。

---

## 7. 测试先锁定（区分验收测试与实现测试）

- **冻结的验收测试**（不得修改）：接口契约测试、安全回归测试、缺陷复现测试、本任务绑定的 `TC-xxx` 验收用例。其断言/阈值/性能口径构成验收标准。
- **实现测试**（可随实现增加/重构）：普通单元测试、内部重构测试——可随代码补充，但**不得降低验收标准**（不得改宽断言、不得把集成改 mock、不得用 skip 绕过）。
- 每个任务开始前先固定验收测试。AI 不得：删除失败测试、降低性能阈值、把断言改宽、把集成测试改成 mock 后的单元测试、用 `@skip` 绕过。
- 必须修改冻结测试时，需单独的测试变更任务（走 Change Request 流程）。

---

## 8. CI 门禁（建议）

格式化、lint、类型检查、单元/集成测试、DB 迁移验证（up/down）、OpenAPI 兼容检查、重复代码扫描、未使用代码扫描、密钥/敏感信息扫描、依赖漏洞扫描、RAG 评测回归。

---

## 9. 事实来源路由与复盘模式（Fact Source Routing，TASK-GOV-SYNC-001 固化）

> 核心区分：**"规范/代码/运行之间谁说了算"**。每条事实只有一个 Owner；不要追求"所有文档同步"，要追求"每种事实只有一个真相源"。

### 9.1 四类真相源

| 你问的问题 | 真相源 | 文档/代码的角色 |
|------------|--------|----------------|
| 产品**应该**怎么工作？ | approved SRS / Domain / OpenAPI / Security | 规范真相（should-be） |
| 当前代码**实际上**怎么工作？ | 指定 Git commit 的源码 + 测试 + migration + 配置 | 实现真相（is） |
| 线上**现在**怎么工作？ | deployed commit + runtime config + logs/metrics | 运行真相 |
| 项目做到哪一步？ | baseline + PROJECT_STATE + TASK | 项目状态真相 |

### 9.2 硬规则：禁止用文档推断实现

- **任何大模型都禁止通过 PRD / SRS 推断"当前代码已经实现了什么"。**
- PRD / SRS 回答的是 **should be**（设计要求）；代码、测试、migration、配置回答的是 **is**（实际实现）。
- 项目复盘必须**同时看二者**才能回答："设计要求什么 → 实际实现什么 → 二者是否有 gap"。

### 9.3 Review / Audit Mode（被问"项目是怎么实现的"时强制启用）

读取顺序（先代码，后规范，最后比对）：
1. 找最后 verified commit（`PROJECT_STATE.md` 的"最后 verified commit"锚点）；
2. 找实际实现：service / repository / transaction / migration / index；
3. 找对应测试（单元/集成/并发）；
4. 得出"**实际实现**"；
5. **再读** approved 的 domain model / SRS，作为"**规范要求**"；
6. 比较设计 vs 实现，输出三栏：**规范要求 / 实际实现 / 差异·技术债**。

**严禁**把"Domain Model 写了 FOR UPDATE"直接说成"项目使用了 FOR UPDATE"——只有代码真的用了，才能这样说。最终答案必须明确区分"规范要求"与"实际实现"，不得混为一谈。

### 9.4 Spec Impact Gate（改变外部行为必须先改规范）

每个代码 TASK 必须填写"规范影响评估"（见 `tasks/TASK-TEMPLATE.md`）。三类判定：
- **代码重构**（行为未变）→ 不需要改 SRS；更新测试/交付证据即可；
- **Bug 修复使代码重新符合现有 SRS** → 不需要改 SRS；更新测试/交付证据；
- **真正改变用户可观察行为** → **不允许直接改代码**；先 Change Request → 更新并 approve 规范 → 再创建 implementation TASK。

本规则是 §1"规格变更、测试期望变更、业务实现不得在同一个 PR 一起完成"的完整展开：**依赖关系变化 → 触发下游 impact review；代码改变外部行为 → 无 Change Request 禁止 merge。**
