# 阿里云生产部署指南（TASK-DEPLOY-001）

> 目标：把 jianli 站点（FastAPI + 预约 Worker + PostgreSQL + Redis + 前端）部署到阿里云轻量应用服务器，域名 + HTTPS 上线，供简历链接访问。
> 状态：**待执行**（用户已批准 TASK-DEPLOY-001，选型 2C4G；本指南为操作手册）

---

## 0. 前置（必须全部完成才能上线）

| # | 事项 | 说明 | 状态 |
|---|------|------|------|
| P1 | **重置泄露凭据** | 2026-08-18 对话中泄露 5 次：163 SMTP 授权码 ×2、飞书 App Secret ×2、飞书授权。**全部重置后**填入 `.env` | ⏳ 待做 |
| P2 | 购买阿里云轻量服务器 | 2C4G、Ubuntu 22.04 LTS、香港或内地地域（内地需备案） | ⏳ 待做 |
| P3 | 购买域名 + ICP 备案 | 内地服务器强制备案（1-2 周）；备案期间可用 IP + 端口临时访问 | ⏳ 待做 |
| P4 | 域名 DNS 解析 | A 记录指向服务器公网 IP | ⏳ 待做 |

---

## 1. 服务器初始化

```bash
# 以 root 或 sudo 用户登录服务器后
sudo apt update && sudo apt upgrade -y
# 安装 Docker + Compose v2
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER   # 重新登录生效
docker --version && docker compose version
```

## 2. 上传代码 + 配置环境

```bash
# 本地构建前端（在你的电脑上）
cd /mnt/c/Users/hxt02/Desktop/jianli
npm run build                    # 产出 dist/，与代码一起上传

# 上传到服务器（任选：git clone / scp / 宝塔面板）
# 以 git 为例：
git clone <your-repo-url> jianli && cd jianli

# 创建生产环境变量文件（服务器上执行，密钥只在这里）
cp apps/api/.env.prod.example .env
nano .env                        # 填入全部真实密钥（见第 0 节 P1）
chmod 600 .env                   # 权限收紧，防误读
```

> ⚠️ `.env` 在服务器上，**不要**提交进 git（根目录 `.gitignore` 已含 `.env`？若无则补）。

## 3. 一键部署

```bash
./scripts/deploy.sh
# 输出校验点：
#   1/4 config OK
#   2/4 build OK
#   3/4 up OK
#   4/4 容器全部 Up (healthy)
```

## 4. 首次数据库迁移（生产库）

```bash
# 在 api 容器内执行 alembic 迁移（0001→0008）
docker compose -f docker-compose.prod.yml exec api \
  alembic upgrade head
```

## 5. 域名 + HTTPS

```bash
# 确认 DNS 已解析到服务器后（第 0 节 P4）：
# ① 把 deploy/nginx.conf 与 .env 中的 <your-domain> 替换为真实域名
sed -i 's/<your-domain>/你的域名/g' deploy/nginx.conf
# ② 申请 Let's Encrypt 证书
./deploy/certbot-init.sh 你的域名 your-email@example.com
# ③ 重启 nginx 生效
docker compose -f docker-compose.prod.yml exec nginx nginx -s reload
```

验证：
```bash
curl -I https://你的域名        # 期望 200 + TLS
curl -I http://你的域名          # 期望 301 → https
```

## 6. 上线验证清单

- [ ] `curl -I https://域名` → 200
- [ ] 前端首页正常渲染（简历问答 / 项目 / 预约）
- [ ] 用浏览器打开站点 → 预约流程走通（创建 / 改期 / 取消）
- [ ] Worker 日志无异常：`docker compose -f docker-compose.prod.yml logs worker | tail`
- [ ] 飞书多维表格：创建一条预约后，表格出现新行（R14）
- [ ] 飞书收到候选人消息（R13，前提 owner_admin 已配置 open_id）
- [ ] 邮箱收到确认函（SMTP 已配新码）

## 7. 运维

| 操作 | 命令 |
|------|------|
| 看状态 | `docker compose -f docker-compose.prod.yml ps` |
| 看日志 | `docker compose -f docker-compose.prod.yml logs -f api worker` |
| 重启全部 | `docker compose -f docker-compose.prod.yml restart` |
| 更新部署 | 拉代码 → `npm run build` → `./scripts/deploy.sh`（重建镜像）|
| 备份 | 数据卷 `jianli_pgdata`（`docker run --rm -v jianli-prod_jianli_pgdata:/data -v $PWD:/backup alpine tar czf /backup/pg.tar.gz /data`）|
| 证书续期 | certbot 已自动续期（compose 未含 cron 时手动每月跑 `certbot-init.sh`）|

## 8. 回滚

```bash
docker compose -f docker-compose.prod.yml down   # 停全部，数据卷保留
# 或回退到上一版镜像：重新 build 旧 commit 后 up
```

## 9. 已知限制与风险

- **备案周期**：内地地域服务器需 ICP 备案（1-2 周），期间域名不可正式对外；可先用 IP:端口验证功能
- **2C4G 容量**：同机 5 容器可跑，但 RAG 问答 + LLM 流式 + 预约并发高时可能吃紧；观测到 CPU 持续 >70% 再考虑升配
- **知识库对象存储**：挂载 `jianli_knowledge` 卷；换机器需迁移该卷
- **备份策略**：当前仅手动备份 PG 数据卷；上线稳定后建议加 cron 自动备份
