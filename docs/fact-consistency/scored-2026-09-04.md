# 简历事实一致率 · 实测评分（2026-09-04 · 真实 LLM + BGE-M3）

> 数据来源：`scripts/fact_consistency_results.json`（38 题真实 Q+A 转录，2026-09-04T08:09:30Z）
> 运行环境：WSL 集成（PG16+pgvector 迁移 0010 / Redis7 / 23 篇语料 BGE-M3 灌库 indexed=23）
> 模型链路：DeepSeek `deepseek-v4-flash`（chat）+ 硅基流动 `BAAI/bge-m3`（embedding 1024 维），`kb_min_score=0.47`
> 评分标尺：`docs/fact-consistency/rubric.md`；题库：`docs/fact-consistency/fact-bank.md`

## 轮次概览

| 轮次 | 时间 | ✅ | ⚠️ | ❌ | 🚫 | 合法拒答 | 严格一致率 | 宽松一致率 | 结论 |
|------|------|----|----|----|----|----------|-----------|-----------|------|
| 2026-09-04 真实 LLM | 08:09Z | 32 | 4 | 2 | 0 | 0 | **84.2%（32/38）** | 94.7%（36/38） | **严格未达 SLO ≥94%（需 ≥36 ✅）** |

- 全部 38 题 `grounded=true`、`offtopic=false`、`error=null`：**检索/域过滤/无误拒层全绿**。
- 严格未达标根因：真实 LLM 的**答案组合未逐项点出期望事实**（事实均在语料/静态兜底内，非检索失败），4 题只答出部分、2 题未提具体期望事实。

---

## 逐题评分（38 题）

### A 组 · 简历域（page_key=`resume`）

| 题号 | 问题 | 评分 | 说明 |
|------|------|------|------|
| FQ-01 | 你主要是什么技术方向的工程师？ | ✅ | "AI 应用开发工程师 + AI Agent 工程/RAG/云边端" |
| FQ-02 | 你平时重点关注哪些技术领域？ | ✅ | "AI Agent 工程、RAG 与云边端系统 + Agent Runtime/工具白名单/HITL" |
| FQ-03 | 你做过哪些类型的系统后端架构？ | ❌ | 答了 AI Agent 后端/RAG 后端/云边端，**未提"预约与协作类系统"**（R10 期望事实在静态兜底内，LLM 未点出） |
| FQ-04 | 你在预约系统里落地过哪些关键设计？ | ✅ | 预览令牌 3 分钟不占 Slot（快照）、事务锁 Slot、delivery 唯一键幂等，命中 ≥2 项 |
| FQ-05 | 你的工程方法论是什么？ | ✅ | "先设计后编码 + 确定性验证" |
| FQ-06 | 你做工程时特别看重什么？ | ✅ | "可观测性、可演进性与契约测试" 全中 |
| FQ-07 | 你的主要技术栈有哪些？ | ✅ | Python/FastAPI + Postgres + Redis + TS 主体全列 |
| FQ-08 | 你熟悉哪些 AI 问答相关技术？ | ✅ | RAG + 受约束 Agent 编排（Planner-Guard-Executor-Synthesizer） |
| FQ-09 | 除了后端架构，你还做过什么方向的功能？ | ❌ | 答成前端/全栈（Litchi 21 页、jianli React、极氪车机），**未提"内容问答与检索"**（R10 期望事实未点出） |
| FQ-10 | 你做的这个站点本质是用来做什么的？ | ✅ | "数字分身 + 回答经历问题 + 承接面试预约" 全中 |

### B 组 · jianli 项目域（page_key=`projects`，project_key=`jianli`）

| 题号 | 问题 | 评分 | 说明 |
|------|------|------|------|
| FQ-11 | jianli 这个项目是做什么的？ | ✅ | AI 面试协作站 + 产品链（RAG 问答/登录/预约/通知） |
| FQ-12 | jianli 处理越界或无依据问题的原则？ | ✅ | "宁可不答、不可编造 + 0.47 证据门 + offtopic" |
| FQ-13 | jianli 的后端技术栈？ | ✅ | FastAPI/SQLAlchemy/Alembic 0010/PG16/pgvector/Redis7 全中 |
| FQ-14 | jianli 用什么模型和 embedding？ | ✅ | DeepSeek V4 Flash + BGE-M3 1024 维 |
| FQ-15 | jianli 的检索是怎么做的？ | ✅ | 向量 top10 + BM25 top10 + RRF top12 + 可选重排 top6 全中 |
| FQ-16 | embedding 换 BGE-M3 后检索质量变化？ | ✅ | "1.8→1.3，BGE-M3 更优" 数值方向正确 |
| FQ-17 | jianli 怎么判断该拒答？ | ⚠️ | 0.47 阈值有；**CJK 停用词过滤层未明确点出** |
| FQ-18 | jianli 的拒答率现在是多少？ | ⚠️ | 诚实答"无生产数据 + 79/79 评测含拒答场景"，**未给期望"100%（REJECT 10/10）"** |
| FQ-19 | jianli 的 Agent 能调用哪些工具？ | ✅ | 五工具 + 本人/管理员 RBAC + BookingService 全中 |
| FQ-20 | Agent 怎么决定要不要检索？ | ✅ | "function calling / tool_choice=auto 模型自主决策" |
| FQ-21 | jianli 用什么量化检索质量？ | ✅ | test_rag_eval 真实语料全链路 + 命中/拒答/隐私/误拒 |
| FQ-22 | jianli 的检索评测达到什么水平？ | ⚠️ | 38/38 + 有限样本边界有；**未提"越界集 10/10 拒答"** |
| FQ-23 | jianli 预约业务闭环关键保障？ | ✅ | 快照/预览不占/原子/加密/Outbox/审计/SKIP LOCKED 5+ 项 |
| FQ-24 | jianli 集成测试情况？ | ✅ | 79/79 六类分组 + 远端未跑边界 |
| FQ-25 | jianli embedding 演进？ | ✅ | "本地哈希 → BGE-M3，哈希无语义 fallback" |
| FQ-26 | jianli 开发中值得记录的坑？ | ⚠️ | 给了 3 个真坑（检索词改写丢证据/阈值/并发唯一索引），**未提期望"greeting 'hi'⊂'litchi' 整词匹配"** |

### C 组 · Litchi 毕设域（page_key=`projects`，project_key=`litchi`）

| 题号 | 问题 | 评分 | 说明 |
|------|------|------|------|
| FQ-27 | litchi 毕设技术栈？ | ✅ | Spring Boot3.2/Java17 + Vue3 + Milvus/Neo4j + 模板边界 |
| FQ-28 | 四段受控 Agent 实现？ | ✅ | Planner-Guard-Executor-Synthesizer + 5 工具 + 4 步 + 7 状态 + HITL |
| FQ-29 | LLM 和向量怎么选？ | ✅ | 哈希向量 + 词法混合 + 非语义 embedding + 159.88ms |
| FQ-30 | 并发压测结果？ | ✅ | 50 并发历史成功 + 100/200 未达标 50.5%/21%/19% |

### D 组 · Sleep 泰益智域（page_key=`projects`，project_key=`sleep202603_an`）

| 题号 | 问题 | 评分 | 说明 |
|------|------|------|------|
| FQ-31 | 睡眠 AI Agent 平台怎么做的？ | ✅ | LangGraph+Temporal Runtime + 5 Agent + Planner-Executor-Validator + 断点恢复 + 白名单/HITL/熔断 + NDA 口径 |
| FQ-32 | 核心指标如何？ | ✅ | 92.0% / 4.1% / 99% + NDA 口径（新口径全对） |
| FQ-33 | 数据链路怎么做？ | ✅ | MQTT→Kafka→ClickHouse + 幂等去重 + 13 秒 + NDA 口径 |

### E 组 · 行为/动机/竞赛

| 题号 | 问题 | 评分 | 说明 |
|------|------|------|------|
| FQ-34 | 你在泰益智是怎么带人的？ | ✅ | 1 对 1 实操→任务+验收→改版 + 还原核心页 |
| FQ-35 | 工程上最大的教训？ | ✅ | 三条全中（评测暴露/静默空结果/登记风险） |
| FQ-36 | 求职动机和职业规划？ | ✅ | 2023 AI 编程 + AI 应用开发 + 深圳南山 + 架构师 |
| FQ-37 | 慧眼识蚁项目是做什么的？ | ✅ | 红火蚁 + 三级架构 + FreeRTOS/MPU6500/PID/RPLIDAR/EC800M/YOLOv5s + ≥95% 目标口径 |
| FQ-38 | 慧眼识蚁做到了什么程度？ | ✅ | 大创立项 + 挑战杯 A 类 + ≥95% 非实测边界 |

---

## 一致率汇总

```
✅ 一致      = 32
⚠️ 部分一致  = 4   （FQ-17 / 18 / 22 / 26）
❌ 不一致    = 2   （FQ-03 / 09）
🚫 误拒      = 0
合法拒答    = 0

严格一致率 = 32/38 = 84.2%   （SLO ≥94% ❌ 未达成，需 ≥36 ✅）
宽松一致率 = 36/38 = 94.7%
编造/冲突  = 0/38  → 真实性优先约束 100% 守住
```

## 未达标根因分析

1. **FQ-03 / FQ-09（❌）**：期望事实"预约与协作类系统""内容问答与检索"均在 `content.py` R10 静态兜底 chunk，但真实 LLM 组合答案时选择了其他检索素材（项目类型 / 前端方向），未逐项点出 R10 具体表述。**素材在、覆盖缺**。
2. **FQ-17 / 18 / 22（⚠️）**：期望事实"0.47 阈值 + CJK 停用词双层""拒答率 100%（REJECT 10/10）"在 `jianli-agent-rag.md` / `jianli-evaluation-ci.md`，LLM 答出主体但漏了具体子项（CJK 停用词、10/10）。
3. **FQ-26（⚠️）**：期望"greeting 'hi'⊂'litchi' 整词匹配"坑未提，LLM 从语料里选了另外 3 个真实坑。

**共性**：不是检索失败（38/38 均有 grounded 依据），而是**真实 LLM 的答案组织未覆盖全部期望事实**。修复方向（均需走内容变更审批，rubric §5 禁止静默改期望凑分）：
- 对 FQ-03/09 类"单一具体事实"问题：检查 R10 chunk 在 resume 域的召回排序，必要时将"预约与协作类系统/内容问答与检索"拆为独立 chunk 或加强锚定；
- 对 FQ-17/18/22/26 类"多子项覆盖"问题：语料中给"CJK 停用词""REJECT 10/10""greeting 整词匹配"增加显眼锚定表述，或调整 persona 风格 few-shot 要求"结论前置 + 逐项覆盖关键事实"。

## 证据与可复跑性

- 复跑命令（WSL 单会话，含容器启动）：`bash _tmp_llm_eval4.sh`（一次性脚本，已用后删除；复跑需重建临时脚本）。
- 环境：`JIANLI_DATABASE_URL=...@127.0.0.1:55432/jianli_tc_aiqa_001_db`、`JIANLI_REDIS_URL=redis://127.0.0.1:63790/0`、`JIANLI_LLM_*`（DeepSeek chat）、`JIANLI_LLM_EMBEDDING_*`（BGE-M3 1024）、`JIANLI_KB_MIN_SCORE=0.47`、`JIANLI_CSRF_HMAC_KEY`/`JIANLI_RATE_LIMIT_HMAC_KEY`（raw ≥32）。
- 重测会覆盖 `scripts/fact_consistency_results.json`；语料/content 变更后须按 rubric §5 走内容变更流程，不可静默改期望值凑分。
