# TASK-KB-EVAL-002 判别式评测集（哈希 vs BGE-M3 语义对比）

> **状态**：Open（2026-08-14 建；用户"收口挂起 3 任务 + 开工"指示）
> **依赖**：TASK-KB-EMB-001 已验证（BGE-M3 接入，HIT=8/8 与哈希持平——字面用例测不出语义差异，本任务解决此问题）

## 1. 背景（为什么需要判别式用例）
EVAL-001 的 8 条命中用例都是**字面关键词命中**（问句直接含文档词），哈希与 BGE-M3 都 8/8——
**测不出语义差异**。语义向量的真实价值要靠**判别式用例**量化：
问句用同义词/异表述（字面与文档零重叠、语义相同）→ 哈希（字符哈希，无语义）必挂，BGE-M3（语义向量）应命中。

## 2. 目标
1. **扩语料** 4→10 篇（增加 top-k 区分度，削弱"候选覆盖全"效应）
2. **新增语义改写型用例**（6-8 条，判别式核心）：
   - 「你在哪所学校读的书？」→ 文档只有「[学校已脱敏]」（字面零重叠，语义同）
   - 「你拿过哪些奖？」→ 文档只有「国家奖学金 / 挑战杯」（字面零重叠）
   - 「Litchi 这个平台的架构怎么设计的？」→ 文档有「Planner→Guard→Executor→Synthesizer」
   - 「睡眠项目里怎么保证长任务不卡死？」→ 文档有「Temporal 长任务编排/失败重试」
3. **扩拒答型** 6→10 条（更多越界/无依据场景）
4. **输出对比数字**：同一评测集跑两遍（哈希 embedding vs BGE-M3 embedding）→
   「哈希 x% vs BGE-M3 y%」的判别式对比 = 简历最硬检索质量证据

## 3. 范围（严格限定）
- ✅ 改写 `tests/aiqa/test_rag_eval.py`（扩语料 + 语义用例 + 扩拒答 + 用例分组标注）
- ✅ 新建任务单本文件
- ❌ 不动 `app/aiqa/**`（无产品代码变更；检索/拒答行为不变）
- ❌ 不加依赖（BGE-M3 走既有 OpenAIEmbeddingGateway，哈希走 LocalEmbeddingGateway）

## 4. 技术要点
- 语料仍是测试内嵌中文 md（模拟简历/两项目 + 新增教育细节/技能/荣誉扩展文档），**不含用户真实个人信息**
- 语义用例的"字面零重叠"用 token 校验保证（测试内 assert 问句 token 与期望文档 token 无交集）
- 对比方法：`JIANLI_LLM_EMBEDDING_*` 有则 BGE-M3、无则哈希（复用 seed_resume_kb 的配置检测思路，测试侧同理——fixture 建 embedder 时按 env 决定）→ 用户跑两遍（配 key / 不配 key）拿对比
- 拒答型仍 xfail（P1 阈值缺陷基线不变，本任务不修检索行为）

## 5. 验收
- [ ] ruff ✅ / mypy ✅（沙箱）
- [ ] WSL：跑两遍（哈希 + BGE-M3）→ 打印对比数字（哈希 HIT vs BGE-M3 HIT）
- [ ] 语义用例在哈希下**必挂**、BGE-M3 下**应过**（判别式有效性验证）
- [ ] 任务单交付证据回填

## 6. 面试价值
"我设计了判别式评测集：同一批 18 条用例（含语义改写型），本地哈希 embedding 命中率 x%，
换 BGE-M3 语义向量后 y%——证明**语义向量对同义表达/模糊提问的鲁棒性是真实可量化的**，
不是玄学。"
