# TASK-UI-001 产出 UI 线框 v1.0（阶段 1 设计工件）

> 复制本模板为 `tasks/TASK-UI-001.md`，作为 AI **仓库变更**的唯一范围约束。无任务单不得写入仓库，包括文档、设计、测试和代码变更。

## 任务类型
- design         # UI 线框（低保真文本线框，非视觉高保真、非代码）

## 基线版本与基线 commit
- baseline：PRD 2.3.3 / 用例规约 1.7.2 / 领域模型 1.1.2 / SRS 1.0（approved）/ AI 治理 1.0.1（取自 `docs/baseline.yml`；SRS 已 approved 成为行为唯一源）
- 基线 commit：173cf9b6ffdf75acc4802398644ba67fb06f6cf6（SRS v1.0 正式 approved 锚点；本任务基于该快照启动）

## 精确规范引用（AI 只读取这些章节）
- SRS v1.0：§3.1–§3.9（全功能域行为）/ §4.1（UI 行为）/ §5（非功能，含配色语义、限频）/ §6.2（状态模型，颜色语义来源）/ §7（权限与角色）
- PRD v2.3.3：§4（功能需求，含 §4.5 面试表网格精确规则、§4.6 通知可靠性）/ §5（非功能）/ §6（验收）/ §8.4（账号隔离、验证码用途）/ §8.6（隐私说明文案）/ §8.9（角色权限矩阵）/ §8.10（状态机枚举）
- 用例规约 v1.7.2：UC-01–UC-23（全用例正常/异常流）
- 领域模型 v1.1.2：§5（状态机规范，颜色语义后端来源）/ §6（实体字段，表单字段来源）

## 需求来源
- R1–R26 全功能需求；UC-01–UC-23 全用例交互；SRS §3 全功能域行为约束

## 目标
产出 UI 线框 v1.0（低保真文本线框）：覆盖 SRS §3.1–§3.9 全部功能域的用户界面，含布局、核心组件、关键交互态与异常态；与 SRS 行为、PRD §4.5 网格规则、§8.9 权限矩阵保持一致；建立页面→用例→需求追踪。

## 非目标（明确排除）
- 不做视觉高保真（不定义具体配色 hex / 字体 / 间距数值，仅定义语义色：绿=空闲/黄=选中临时态/红=不可约或已约）
- 不做架构选型 / ADR 编写
- 不写前端代码 / 组件实现
- 不定义 REST URL / OpenAPI / SSE payload（留待接口契约）
- 不修改 PRD / 用例 / 领域模型 / SRS 业务内容

## 允许修改路径
- docs/design/ui-wireframe.md   # 新建 UI 线框 v1.0（本任务主交付物）
- tasks/TASK-UI-001.md          # 本任务单自身回填交付证据

## 禁止修改路径
- PRD / 用例规约 / 领域模型 / SRS 业务内容（仅引用，不改）
- docs/experiments/deferred/l2-persona-training.md
- docs/references/agent-engineering-frameworks.md
- 任何代码文件、数据库迁移、OpenAPI / SSE 契约

## 已批准的 DB / API / 依赖变更
- 无（本任务纯设计，不含 schema / API / 依赖变更）

## 功能验收
- 线框覆盖 SRS §3.1–§3.9 全功能域界面；页面清单与 SRS 功能域一一映射
- 面试表页线框忠实体现 PRD §4.5 网格规则（7 列 × 25 行 × 14 天窗口、三色语义、黄格不落库、选段自动占 1.5h、最短提前量）
- 预约流程线框体现 §3.5 两段式（表单不落库 → 二次确认限时 3 分钟 → 原子锁写入）+ §3.6 改期原子事务
- 登录/注册/后台页面体现 §8.4 账号隔离、§8.9 角色权限（面试官 vs admin 只读应急）
- 全仓 Grep 校验：线框中语义色与 SRS §6.2 状态模型一致（available/booked/owner_locked/unavailable）

## 安全与隐私验收
- 红格对他人仅显"已预约"（无公司名），线框标注隐私遮挡（R16）
- 表单敏感字段线框标注"加密存储、网站端不展示明文"（R16）
- 二次确认收件邮箱只读不可编辑（R26），线框标注

## 性能验收
- 不适用（线框为静态设计）；非功能约束（SSE≤2s 刷新、首字≤3s）在线框中以注释标注，供后续实现参考

## 变更预算（change_budget）
- max_files：2（ui-wireframe.md + 本任务单）
- expected_prod_lines：线框正文为主
- expected_test_lines：0

## 必须运行的测试命令
- 无（设计任务）；交付前执行全仓一致性 Grep 校验（语义色 / 页面-用例映射）

## 回滚方法
- 初始化 Git 后使用 git restore/revert；本任务不产生迁移

## 强制停止条件（与 `AGENTS.md §2` 一致）
判定口径：**看变更是否已在本任务单「允许修改路径」列明，而不是看变更类型本身。**
- **可继续**：变更已在「允许修改路径」列明且为设计性质，依据工件（SRS）在 `docs/baseline.yml` 为 `approved`。
- **必须立即停止并报告（不得自行决定）**：出现任何未在「允许修改路径」列明的变化，包括新增/修改数据库表、字段、索引；新增/修改公开 API；改变加密/鉴权/权限策略；修改 SRS 业务内容；实现任务范围外功能（含 deferred 延后项）。

## 交付证据（任务关闭前必须填写，缺一不得关闭）
- commit / PR：30afe97b562f73e9166b93761416b43633c567ed（UI 线框 v1.0 初稿 + TASK-UI-001 创建）
- 修改文件清单：<回填，与「允许修改路径」对照>
- 测试命令及结果：全仓一致性 Grep 校验（语义色 / 页面-用例映射）→ <回填>
- lint / typecheck：不适用（设计任务）
- DB 迁移验证：无
- 验收证据：<页面清单 + 每页 SRS/UC 映射 + 关键交互流线框>
- 变更预算实际值：<回填>
- 未解决风险：<或「无」>
- 是否偏离 TASK：<否 / 偏离项>
- 规范影响结论：none（纯设计，不改业务行为；SRS 已 approved 为上游依据）
- spec_sync：clean（SRS 已 approved 且 based_on 未变，UI 线框为下游设计、不反向改 SRS）
- verified_commit：30afe97b562f73e9166b93761416b43633c567ed（UI 线框 v1.0 初稿提交；任务未关闭，待评审批准后更新为批准锚点）
- **关闭门禁（四条件全满足方可关闭）**：① 测试通过；② 规范影响已处理（none）；③ spec_sync = clean；④ verified_commit 已记录真实 sha。

## 阶段性证据
- UI 线框 v1.0 初稿已完成（commit `30afe97b`），覆盖 U1–U12（用户页面）+ A1–A8（admin 后台），与 SRS §3.1–§3.9 功能域一一映射；语义色（绿=available / 黄=前端临时态不落库 / 红=booked/owner_locked/unavailable）与 SRS §6.2 状态模型一致。
- 关键交互流（预约原子创建、改期原子事务、owner 强制取消）已按 §3.5/§3.6/§3.7 绘制。
- baseline `ui_wireframe` 仍 `pending`；本任务未关闭，待用户/独立评审批准 v1.0 后将 `ui_wireframe.status` 置 `approved` 并补全关闭证据。

## 关联
- Change Request：无
- 测试任务：无（设计）
